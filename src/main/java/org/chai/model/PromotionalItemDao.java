package org.chai.model;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;
import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

import java.util.ArrayList;
import java.util.List;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table PROMOTIONAL_ITEM.
*/
public class PromotionalItemDao extends AbstractDao<PromotionalItem, String> {

    public static final String TABLENAME = "PROMOTIONAL_ITEM";

    /**
     * Properties of entity PromotionalItem.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Uuid = new Property(0, String.class, "uuid", true, "UUID");
        public final static Property Name = new Property(1, String.class, "name", false, "NAME");
        public final static Property PromotionId = new Property(2, String.class, "promotionId", false, "PROMOTION_ID");
    };

    private DaoSession daoSession;

    private Query<PromotionalItem> promotion_ItemsQuery;

    public PromotionalItemDao(DaoConfig config) {
        super(config);
    }
    
    public PromotionalItemDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'PROMOTIONAL_ITEM' (" + //
                "'UUID' TEXT PRIMARY KEY NOT NULL UNIQUE ," + // 0: uuid
                "'NAME' TEXT NOT NULL ," + // 1: name
                "'PROMOTION_ID' TEXT NOT NULL );"); // 2: promotionId
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'PROMOTIONAL_ITEM'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, PromotionalItem entity) {
        stmt.clearBindings();
        stmt.bindString(1, entity.getUuid());
        stmt.bindString(2, entity.getName());
        stmt.bindString(3, entity.getPromotionId());
    }

    @Override
    protected void attachEntity(PromotionalItem entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.getString(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public PromotionalItem readEntity(Cursor cursor, int offset) {
        PromotionalItem entity = new PromotionalItem( //
            cursor.getString(offset + 0), // uuid
            cursor.getString(offset + 1), // name
            cursor.getString(offset + 2) // promotionId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, PromotionalItem entity, int offset) {
        entity.setUuid(cursor.getString(offset + 0));
        entity.setName(cursor.getString(offset + 1));
        entity.setPromotionId(cursor.getString(offset + 2));
     }
    
    /** @inheritdoc */
    @Override
    protected String updateKeyAfterInsert(PromotionalItem entity, long rowId) {
        return entity.getUuid();
    }
    
    /** @inheritdoc */
    @Override
    public String getKey(PromotionalItem entity) {
        if(entity != null) {
            return entity.getUuid();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "items" to-many relationship of Promotion. */
    public List<PromotionalItem> _queryPromotion_Items(String promotionId) {
        synchronized (this) {
            if (promotion_ItemsQuery == null) {
                QueryBuilder<PromotionalItem> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.PromotionId.eq(null));
                promotion_ItemsQuery = queryBuilder.build();
            }
        }
        Query<PromotionalItem> query = promotion_ItemsQuery.forCurrentThread();
        query.setParameter(0, promotionId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getPromotionDao().getAllColumns());
            builder.append(" FROM PROMOTIONAL_ITEM T");
            builder.append(" LEFT JOIN PROMOTION T0 ON T.'PROMOTION_ID'=T0.'UUID'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected PromotionalItem loadCurrentDeep(Cursor cursor, boolean lock) {
        PromotionalItem entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Promotion promotion = loadCurrentOther(daoSession.getPromotionDao(), cursor, offset);
         if(promotion != null) {
            entity.setPromotion(promotion);
        }

        return entity;    
    }

    public PromotionalItem loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<PromotionalItem> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<PromotionalItem> list = new ArrayList<PromotionalItem>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<PromotionalItem> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<PromotionalItem> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
